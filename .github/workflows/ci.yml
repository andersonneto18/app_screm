name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
        env:
          # postinstall runs `prisma generate`; it reads the config, not the DB.
          DATABASE_URL: postgresql://user:pass@localhost:5432/db
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run test
